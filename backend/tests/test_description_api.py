"""
Tests for description API.

NOTE: These tests are obsolete. The /api/description endpoints have been removed
from the application. These tests now reference non-existent models (Description, ImageAnalysis).

Keeping the file but all tests are skipped since the functionality no longer exists.
"""

import pytest


def test_description_api_obsolete():
    """
    The description API has been removed from the application.
    Tests skipped as functionality no longer exists.
    """
    pytest.skip("Description API has been removed - these tests are obsolete")
